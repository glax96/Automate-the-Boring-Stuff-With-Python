1. spam{}

2. spam={'foo': 42}

3. dictionary moze sadrzati iteme,druge liste i druge dictionarye unutar sebe a lista
   samo iteme i druge liste

4. dobijamo KeyError : 'foo' zato što ne postoji key unutar dictionarya pod nazivom 
   'foo'

5. Nema razlike osim u slučaju ako je 'cat' u tom rječniku sam ako nije spam.keys() javlja error

6. Nema razlike osim u slučaju ako je 'cat' u tom rječniku sam ako nije spam.values() javlja error

7. spam.setdefault('color','black')

8. pprint.pprint(), print(pprint.pformat())